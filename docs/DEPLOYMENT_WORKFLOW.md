# Полный Workflow Разработки с Test Agent

## 🎯 Цель

Автоматизировать процесс тестирования и деплоя через Yandex Tracker и Test Agent.

## 📋 Рекомендуемый Workflow

```
Разработка → Тестирование → Деплой → Проверка
```

### Детальный процесс:

```
1. Код написан ✅
   ↓
2. Задача переведена в "В работе" (inProgress)
   ↓
3. Test Agent получает webhook/проверяет задачи
   ↓
4. Запускаются автоматические тесты
   ↓
   ├─ Тесты прошли ✅
   │  ↓
   │  └─ Задача закрывается автоматически
   │     ↓
   │     └─ Готово к деплою
   │
   └─ Тесты не прошли ❌
      ↓
      └─ Задача возвращается в "Открыта"
         ↓
         └─ Исправление → снова "В работе"
```

## 🚀 Настройка среды

### 1. Установка зависимостей

```bash
cd /path/to/exam
pip install -r requirements.txt
```

### 2. Настройка .env

```bash
# Скопируйте .env.example в .env
cp .env.example .env

# Заполните необходимые переменные
YANDEX_TRACKER_TOKEN=your_token
YANDEX_TRACKER_ORG_ID=your_org_id
YANDEX_TRACKER_QUEUE=EXTEST
```

### 3. Запуск Test Agent

#### Вариант А: Webhook режим (рекомендуется)

```bash
# Запустите webhook agent
python scripts/run_webhook_test_agent.py

# В отдельном терминале (для локальной разработки с ngrok):
ngrok http 8080

# Настройте триггер в Yandex Tracker с ngrok URL
# См. docs/TRACKER_TRIGGERS.md
```

#### Вариант Б: Polling режим

```bash
# Простой запуск без настройки триггеров
python scripts/run_test_agent.py
```

## 📝 Процесс работы с задачами

### Шаг 1: Создание задачи

В Yandex Tracker:
1. Создайте новую задачу в очереди EXTEST
2. Опишите требования
3. Статус: "Открыта"

### Шаг 2: Разработка

```bash
# Создайте ветку
git checkout -b feature/EXTEST-XX

# Пишите код
# Создавайте тесты

# Коммитьте
git add .
git commit -m "EXTEST-XX: Описание изменений"
```

### Шаг 3: Локальное тестирование

```bash
# Запустите тесты локально
pytest tests/unit/ -v

# Проверьте coverage
pytest tests/unit/ --cov=src --cov-report=term-missing

# Coverage должен быть >= 80%
```

### Шаг 4: Перевод задачи "В работе"

1. Откройте задачу в Yandex Tracker
2. Переведите в статус "В работе" (inProgress)
3. **Test Agent автоматически**:
   - Получит уведомление (webhook) или обнаружит задачу (polling)
   - Запустит тесты для этой задачи
   - Проверит coverage
   - Добавит комментарий с результатами

### Шаг 5: Результат тестирования

#### ✅ Если тесты прошли:

- Test Agent автоматически закроет задачу
- В комментарии будет: "✅ Тесты успешно пройдены"
- Задача готова к деплою

```bash
# Можно мержить в main
git checkout main
git merge feature/EXTEST-XX
git push origin main
```

#### ❌ Если тесты не прошли:

- Test Agent вернёт задачу в статус "Открыта"
- В комментарии будет: "❌ Тесты не прошли" с деталями
- Исправьте код и повторите шаг 4

```bash
# Исправьте проблемы
# Коммитьте исправления
git add .
git commit -m "EXTEST-XX: Исправление тестов"

# Снова переведите задачу "В работе" в Tracker
```

## 🔄 Деплой

### После успешного тестирования

```bash
# 1. Запустите deployment script
python scripts/deploy.py

# Deployment script автоматически:
# - Проверит окружение
# - Установит зависимости
# - Запустит ВСЕ тесты
# - Проверит coverage
# - Подготовит приложение к запуску
```

### Запуск бота в продакшене

```bash
# Запустите бота
python -m src.main

# Или через systemd (Linux)
sudo systemctl start voice-calendar-bot
```

## 🎛️ Мониторинг

### Проверка статуса Test Agent

```bash
# Webhook режим
curl http://localhost:8080/health

# Логи
tail -f logs/test_agent.log
```

### Проверка задач в Tracker

1. Откройте очередь EXTEST
2. Фильтр: "Закрыта" - успешно протестированные задачи
3. Фильтр: "Открыта" - требуют исправления

## 📊 Метрики качества

### Требования для закрытия задачи:

- ✅ Все тесты проходят
- ✅ Coverage >= 80%
- ✅ Код прошёл review (опционально)
- ✅ Нет критических замечаний от линтеров

## 🔧 Troubleshooting

### Test Agent не срабатывает

**Webhook режим:**
```bash
# Проверьте, что agent запущен
curl http://localhost:8080/health

# Проверьте триггер в Tracker
# - Триггер включен?
# - URL webhook правильный?
# - ngrok туннель активен?

# Тестируйте webhook локально
python scripts/test_webhook_local.py
```

**Polling режим:**
```bash
# Проверьте логи
grep "Checking for tasks" logs/test_agent.log

# Убедитесь, что задача в статусе inProgress
```

### Тесты падают локально, но должны проходить

```bash
# Очистите cache
pytest --cache-clear

# Проверьте зависимости
pip install -r requirements.txt --upgrade

# Проверьте .env файл
cat .env | grep REQUIRED_VAR
```

### Coverage ниже 80%

```bash
# Посмотрите детальный отчёт
pytest tests/unit/ --cov=src --cov-report=html
open htmlcov/index.html

# Найдите непокрытые строки
pytest tests/unit/ --cov=src --cov-report=term-missing

# Добавьте тесты для непокрытого кода
```

## 🎉 Примеры

### Пример 1: Новая фича с автотестами

```bash
# 1. Создали задачу EXTEST-30 в Tracker
# 2. Разработка
git checkout -b feature/EXTEST-30
# ... пишем код и тесты ...
git commit -m "EXTEST-30: Добавлен новый провайдер"

# 3. Локальное тестирование
pytest tests/unit/test_new_provider.py -v
# ✅ 10 passed

# 4. Переводим задачу в "В работе" в Tracker
# Test Agent автоматически тестирует...

# 5. Test Agent закрывает задачу ✅
# Готово! Можно деплоить
```

### Пример 2: Исправление бага

```bash
# 1. Задача EXTEST-31 создана в Tracker (баг в calendar_aggregator)
# 2. Воспроизводим баг локально
# 3. Пишем тест, который воспроизводит баг
pytest tests/unit/test_calendar_aggregator.py::test_bug_fix
# ❌ FAILED - баг подтверждён

# 4. Исправляем код
# 5. Тест теперь проходит
pytest tests/unit/test_calendar_aggregator.py::test_bug_fix
# ✅ PASSED

# 6. Переводим EXTEST-31 в "В работе"
# Test Agent тестирует и закрывает ✅
```

## 🚦 Best Practices

1. **Всегда пишите тесты** вместе с кодом
2. **Тестируйте локально** перед переводом задачи
3. **Используйте webhook режим** для быстрого feedback
4. **Не закрывайте задачи вручную** - пусть Test Agent делает это
5. **Проверяйте комментарии** Test Agent для понимания причин провала
6. **Поддерживайте coverage** >= 80%

## 📚 Дополнительные ресурсы

- [Настройка триггеров](TRACKER_TRIGGERS.md)
- [API Yandex Tracker](https://cloud.yandex.com/en/docs/tracker/concepts/)
- [pytest документация](https://docs.pytest.org/)
- [GitHub Actions CI/CD](.github/workflows/ci.yml)
