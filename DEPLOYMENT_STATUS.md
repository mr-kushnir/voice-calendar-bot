# Статус развертывания на Yandex Cloud

**Дата:** 2025-11-05
**Статус:** ⚠️ ГОТОВО К РАЗВЕРТЫВАНИЮ (блокировано временными проблемами сети)

---

## ✅ Выполнено

### 1. Инфраструктура развертывания создана

**Файлы:**
- `Dockerfile` - Контейнеризация приложения (Python 3.11-slim)
- `docker-compose.yml` - Оркестрация сервисов (bot, test-agent, webhook-agent)
- `.dockerignore` - Оптимизация build context
- `scripts/deploy_yandex_cloud.sh` - Bash скрипт автоматического развертывания
- `scripts/deploy_yandex_cloud.py` - Python скрипт автоматического развертывания (кроссплатформенный)
- `docs/YANDEX_CLOUD_DEPLOYMENT.md` - Полное руководство по развертыванию
- `README.md` - Обновлен с инструкциями по Docker и деплою

**Commit:** `0ed1133`
**GitHub:** https://github.com/mr-kushnir/voice-calendar-bot

### 2. Yandex Cloud конфигурация проверена

**Настройки:**
- ✅ Yandex Cloud CLI установлен: версия 0.169.0
- ✅ Cloud ID: b1gceark3l1k4oj90acu
- ✅ Folder ID: b1gm1nh37o3isrorujke
- ✅ Network: enpa3gbb6lddvdaihjil (default)
- ✅ Subnet: e9b4i1js8g2tol33omp8 (ru-central1-a, 10.128.0.0/24)
- ✅ SSH ключ: ~/.ssh/id_rsa.pub существует
- ✅ .env файл: настроен

### 3. Предварительные проверки пройдены

```
✅ Yandex Cloud CLI - OK
✅ Docker - OK (v28.3.2)
✅ Folder ID - OK
✅ Subnet ID - OK
✅ .env file - OK
✅ SSH key - OK
```

---

## ❌ Текущая блокировка

### Проблема: Debian репозитории недоступны (503 Service Unavailable)

**Описание:**
При сборке Docker образа возникают ошибки 503 при загрузке пакетов из http://deb.debian.org/debian/:

```
Failed to fetch:
- binutils-common_2.44-3_amd64.deb
- libasan8_14.2.0-19_amd64.deb
- liblerc4_4.0.0+ds-5_amd64.deb
- libtiff6_4.7.0-3+deb13u1_amd64.deb
- и другие...
```

**Причина:**
Временные проблемы с сетью/зеркалами Debian. Не связано с нашим кодом или конфигурацией.

**Попытки:**
1. Первая попытка (with ffmpeg): Failed - 503 errors
2. Вторая попытка (without ffmpeg, only gcc): Failed - 503 errors
3. Третья попытка (via deployment script): Failed - same issue

---

## 🔧 Варианты решения

### Вариант 1: Дождаться восстановления зеркал Debian (рекомендуется)

**Действия:**
1. Подождать 30-60 минут
2. Повторить команду:
   ```bash
   export YC_FOLDER_ID="b1gm1nh37o3isrorujke"
   export YC_SUBNET_ID="e9b4i1js8g2tol33omp8"
   python scripts/deploy_yandex_cloud.py
   ```

**Плюсы:**
- Простое решение
- Нет необходимости менять код

**Минусы:**
- Нужно ждать

### Вариант 2: Использовать другие зеркала Debian

**Действия:**
Обновить Dockerfile:
```dockerfile
# Before apt-get update
RUN sed -i 's|http://deb.debian.org|http://ftp.de.debian.org|g' /etc/apt/sources.list.d/debian.sources
```

### Вариант 3: Использовать Alpine Linux вместо Debian

**Действия:**
Изменить базовый образ в Dockerfile:
```dockerfile
FROM python:3.11-alpine
RUN apk add --no-cache gcc musl-dev
```

**Плюсы:**
- Меньший размер образа
- Другие репозитории

**Минусы:**
- Может потребоваться корректировка зависимостей

### Вариант 4: Деплой без Docker (прямо на VM)

**Действия:**
1. Создать VM через веб-консоль Yandex Cloud
2. SSH на VM
3. Установить Python и зависимости
4. Запустить бота через systemd

**Плюсы:**
- Не зависит от Docker build

**Минусы:**
- Менее изолированное окружение
- Сложнее управлять

---

## 📋 Инструкции для деплоя (когда Debian будет доступен)

### Автоматический деплой (рекомендуется)

```bash
# Установить переменные окружения
export YC_FOLDER_ID="b1gm1nh37o3isrorujke"
export YC_SUBNET_ID="e9b4i1js8g2tol33omp8"

# Запустить деплой
python scripts/deploy_yandex_cloud.py
```

### Ручной деплой

```bash
# 1. Собрать Docker образ
docker build -t voice-calendar-bot:latest .

# 2. Экспортировать образ
docker save voice-calendar-bot:latest -o voice-calendar-bot.tar

# 3. Создать VM
yc compute instance create \
  --name voice-calendar-bot \
  --folder-id b1gm1nh37o3isrorujke \
  --zone ru-central1-a \
  --platform standard-v3 \
  --cores 2 \
  --memory 2GB \
  --create-boot-disk size=20GB,image-family=ubuntu-2204-lts \
  --network-interface subnet-id=e9b4i1js8g2tol33omp8,nat-ip-version=ipv4 \
  --ssh-key ~/.ssh/id_rsa.pub

# 4. Скопировать файлы на VM
# (см. полную инструкцию в docs/YANDEX_CLOUD_DEPLOYMENT.md)
```

---

## 🎯 Ожидаемый результат

После успешного деплоя:

1. **VM создан** в Yandex Cloud:
   - Name: voice-calendar-bot
   - Zone: ru-central1-a
   - Resources: 2 vCPU, 2 GB RAM, 20 GB disk

2. **Бот запущен** в Docker контейнере:
   - Автостарт через systemd
   - Логирование в /opt/voice-calendar-bot/logs
   - Health checks

3. **Доступ для управления:**
   ```bash
   ssh ubuntu@<VM_IP>
   cd /opt/voice-calendar-bot
   sudo docker-compose ps
   sudo docker-compose logs -f
   ```

---

## 📊 Метрики

**Созданная инфраструктура:**
- Файлов: 7 (Dockerfile, docker-compose.yml, 2 скрипта деплоя, документация)
- Строк кода: 1224+
- Commit: 0ed1133
- Готовность: 95% (блокировано только сборкой Docker из-за сети)

**Время выполнения (оценка):**
- Сборка Docker: ~5-10 мин (когда Debian доступен)
- Создание VM: ~2-3 мин
- Копирование файлов: ~1-2 мин
- Настройка на VM: ~2-3 мин
- **Итого:** ~10-18 мин

**Стоимость:**
- VM (2 vCPU, 2 GB): ~900-1200 ₽/месяц (~$10-13/месяц)

---

## ✅ Следующие шаги

1. ⏳ Дождаться восстановления Debian репозиториев
2. 🚀 Запустить `python scripts/deploy_yandex_cloud.py`
3. ✅ Проверить работу бота на VM
4. 📝 Обновить DNS (опционально)
5. 📊 Настроить мониторинг (опционально)

---

## 📚 Документация

- **Полное руководство:** [docs/YANDEX_CLOUD_DEPLOYMENT.md](docs/YANDEX_CLOUD_DEPLOYMENT.md)
- **README:** [README.md](README.md)
- **GitHub:** https://github.com/mr-kushnir/voice-calendar-bot

---

**Подготовил:** Claude Code
**Дата:** 2025-11-05
**Статус:** ⚠️ Готово к развертыванию (ожидание восстановления сети)

🎯 **Вывод:** Вся инфраструктура для развертывания на Yandex Cloud создана и готова. Деплой заблокирован только временными проблемами с Debian репозиториями. После восстановления сети деплой займет ~10-18 минут.
